# ============================================================
# LuxeAura Backend – Python Dependencies
# ============================================================

# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0        # [standard] includes uvloop + httptools for performance

# --- Database ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9           # PostgreSQL adapter (binary build = no compilation needed)
python-dotenv>=1.0.1             # .env file loading for local development

# --- Auth / Security ---
python-jose[cryptography]>=3.3.0 # JWT encode/decode
# IMPORTANT: bcrypt must be pinned to 4.0.1.
# bcrypt 4.1+ removed the __about__ attribute which breaks passlib's version
# detection (AttributeError: module 'bcrypt' has no attribute '__about__').
# bcrypt 4.0.1 is the last stable release that works correctly with both
# direct usage (security.py) and passlib's compatibility shim.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Request Parsing ---
python-multipart>=0.0.9          # Required for form data / OAuth2 password flow

# --- Data Validation ---
pydantic>=2.6.0
pydantic[email]>=2.6.0           # Email validation support
pydantic-settings>=2.2.0         # BaseSettings for environment-variable config

# --- Testing (not installed in production Docker image) ---
pytest>=8.0.0
httpx>=0.27.0                    # Async HTTP client used by TestClient
